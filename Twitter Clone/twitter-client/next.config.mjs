/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["localhost", "czzkufhubdhqzbdhwljm.supabase.co", "plus.unsplash.com", "media.istockphoto.com"]
    },
};

export default nextConfig;